package LabAssignment_CLA;

public class A2Q2 {
	public static void main(String[] args) {
		int fist = Integer.parseInt(args[0]);
		int second = Integer.parseInt(args[1]);
		
		System.out.println("first number raise to the power of second number : " + (int)Math.pow(fist, second));
	}
}
